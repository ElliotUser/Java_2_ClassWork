1.Что такое cast и instanceOf?
Cast - приведение типов.
instanceOf - это проверка на то, является ли конкретный объект, объектом конкретного класса.
2. Где хранятся объекты и примитывы?
примитивы хранятся в стеке но могут храниться и в куче.
объекты хранятся в КУЧЕ.
В локальные переменные находятся в стеке а все остальное находится в куче. Локальная переменная
это так конорая находится в методе и это метод сейчас исполняется.
3. Аргументы переменной длинны.

План разработки приложения
Задача.
Создаем приложение которое будет открывать окошко, на окошке будет панелька. По панельке будут летать
шарики и они будут отталкиваться от граней окошка.

Создадим класс MainCircles конторый будет наследоваться от JFrame.
В данном классе создадим 4 константных переменных типа int. POS_X POS_Y WINDOW_HEIGHT WINDOW_WIDTH
